fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
httpx>=0.27
pyyaml>=6.0
ddgs>=9
sentence-transformers>=3.0
# Офлайн-перевод запроса RU→EN для научного поиска (arXiv/Crossref/S2 —
# англоязычные). Модель ru→en скачивается в образ при сборке, в проде
# работает без сети: данные запроса никуда не уходят (важно для приватности)
argostranslate>=1.9
