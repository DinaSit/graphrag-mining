# Локальные артефакты ML-A: результаты прогонов и байткод в репозиторий не попадают.
# README.md и scripts/probe_api.py — часть репозитория: на README ссылаются
# сообщения об ошибках, probe_api — штатная диагностика доступа к API.
results/
__pycache__/
