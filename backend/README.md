# Backend

FastAPI-сервис (порт 8000): приём документов, конвейер обработки, вопросно-ответный
оркестратор и веб-интерфейс «Графа». Полное описание системы, таблицы API
и переменных окружения — в [корневом README](../README.md).

## Модули

| Файл | Назначение |
| --- | --- |
| `app/main.py` | HTTP-эндпоинты и composition root: собирает `ApplicationStore` с адаптерами PostgreSQL/Neo4j/MinIO, очередь загрузок, `QueryOrchestrator`; монтирует `/ui` |
| `app/storage.py` | `ApplicationStore` — конвейер инжеста и рабочее состояние приложения в памяти процесса (документы, фрагменты, кандидаты, факты, векторы); выбор провайдеров по переменным окружения. Единственная точка изменения данных: восстановление после перезапуска, проекция графа и сборка факта вынесены в `pipeline/` |
| `app/persistence.py` | `PostgresSink` (персистентная копия состояния, восстановление при старте, идемпотентные `ALTER` в `ensure_schema`) и `Neo4jSink` (проекция графа знаний) |
| `app/file_storage.py` | Адаптер MinIO: оригиналы документов и их PDF-превью |
| `app/jobs.py` | Очередь фоновой обработки загрузок; статусы задач в памяти, персистентная копия в таблице `jobs` |
| `app/schemas.py` | Pydantic-модели API и домена; `ONTOLOGY_LABELS` — единственный источник меток Neo4j |

### app/pipeline/

| Файл | Назначение |
| --- | --- |
| `query.py` | `QueryOrchestrator`: последовательность шагов ответа — быстрая ветка и полный пайплайн, обе сводятся к `CollectedEvidence`, затем `finalize` собирает `QueryResponse` (общий путь для `/ask` и `/ask/stream`) |
| `query_routing.py` | Решения до запуска конвейера: относится ли вопрос к предметной области, достаточно ли быстрой ветки вместо графа с планировщиком |
| `query_parsing.py` | Разбор вопроса в структурированный запрос средствами LLM: материал, процесс, условия, сущности онтологии |
| `fact_selection.py` | Отбор фактов под вопрос: пословный матчинг, фильтры и ранжирование, косвенный поиск при отсутствии прямых данных, представления фактов (строки таблицы, источники) |
| `graph_traversal.py` | Обход Neo4j тремя Cypher-шаблонами (сущности, числовые параметры, регион) и проверка числовых условий по payload кандидатов |
| `evidence.py` | `CollectedEvidence`, сборка пронумерованного evidence pack и вызов модели: промпт ответа и сообщения генерации |
| `citations.py` | Цитаты ответа: перевод номеров модели в канонические идентификаторы фрагментов и разрешение их в источники |
| `answer_analysis.py` | Выводы конвейера без модели: противоречия при сопоставимых условиях, пробелы, статус доказательной базы, ответ при недоступной LLM |
| `graph_view.py` | Проекция фактов в граф для интерфейса: узлы Claim/Material/Process/Equipment/Document, дедупликация и подписи |
| `fact_builder.py` | Сборка факта из утверждённого кандидата и проверка качества кандидата |
| `hydration.py` | Восстановление состояния из PostgreSQL при старте и бэкфилы (признаки документов, год, гигиена фактов, переиндексация, PDF-превью) |
| `parsers.py` | Парсеры форматов: PDF (включая рендер сканов в PNG), DOCX, PPTX, XLSX/CSV, TXT/MD/JSON; нераспознанное содержимое регистрируется фрагментом-заглушкой |
| `document_traits.py` | Признаки документа: тип и научность определяет LLM по титульным страницам с обоснованием-цитатой; происхождение и год — эвристики без LLM, при расхождении приоритет у имени файла |
| `office_render.py` | PDF-превью DOCX/DOCM/PPTX через LibreOffice; при сбое превью не создаётся, документ остаётся доступен текстом |
| `providers.py` | Провайдеры извлечения и эмбеддингов: удалённые (ml-extraction) и автономные (заглушка, hash-эмбеддер) |
| `validation.py` | Пороги кандидатов и диапазоны правдоподобия чисел из `domain/default/validation-rules.yaml` |
| `normalization.py` | Канонизация текста, идентификаторы, направления эффекта, единый список недопустимых подписей — функции, общие для слоёв запросов и хранения |
| `llm_bridge.py` | HTTP-мост к LLM через ml-extraction (`/chat_json`, `/chat_stream`); каскад провайдеров реализован на стороне сервиса |
| `dyk.py` | Рубрика «Знаете ли вы?»: LLM-формулировка фактов с кэшем и фоновым прогревом |

## Веб-интерфейс (ui/)

Одностраничное приложение без сборки и внешних зависимостей: маршрутизация по
hash, данные API вставляются только через `textContent`/`createElement`.
Просмотрщик pdf.js в репозитории не хранится: `backend/Dockerfile` устанавливает
его при сборке образа в `ui/pdfjs/`.

`index.html` содержит только разметку, оформление — в `styles.css`, код — в
ES-модулях `js/`, которые браузер загружает сам (сборщик не нужен). Точка
входа — `js/main.js`: он импортирует остальные модули и выполняет стартовые
вызовы.

| Модуль | Назначение |
| --- | --- |
| `js/main.js` | Точка входа: подключение модулей и старт (опрос здоровья, счётчик Review, реестр веб-источников, список документов, первая отрисовка) |
| `js/dom.js` | Утилиты создания и поиска элементов |
| `js/state.js` | Состояние приложения `S`, кэш документов, недавние вопросы |
| `js/router.js` | Hash-роутинг и отрисовка текущего экрана |
| `js/ask.js` | Запуск вопроса: отмена прежнего стрима, старт основного и веб-контура |
| `js/article_stream.js` | Состояние статьи и приём SSE-событий `/ask/stream` |
| `js/citations.js` | Живые сноски: замена идентификаторов фрагментов номерами |
| `js/footnote_popup.js` | Всплывающая карточка сноски |
| `js/article_toc.js` | Оглавление статьи |
| `js/view_article.js` | Экран статьи, скролл-spy оглавления, оформление вопроса вне предметной области |
| `js/web_mode.js` | Режим ответа по внешним источникам и реестр площадок |
| `js/graph.js` | Граф знаний: продольная раскладка и полноэкранный просмотр |
| `js/view_home.js` | Главная: поиск, «Знаете ли вы?», популярное и недавнее |
| `js/view_sources.js` | Экран источников с просмотрщиком документа и подсветкой цитаты |
| `js/view_docs.js` | Экран «Документы»: список, фильтры, досье документа |
| `js/view_review.js` | Экран Review и красная точка непроверенных кандидатов |
| `js/health.js` | Статус систем в шапке |
| `js/upload.js` | Загрузка документов и плашка прогресса |
| `js/header.js` | Поиск в шапке, горячие клавиши, бургер-меню |

## Миграции

`migrations/001_init.sql` применяется однократно, при первом создании тома
PostgreSQL (`docker-entrypoint-initdb.d`). Последующие изменения схемы выполняет
`PostgresSink.ensure_schema` идемпотентными `ALTER` при старте сервиса; новые
файлы миграций не добавляются.
